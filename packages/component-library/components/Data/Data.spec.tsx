import { cleanup, render } from "@testing-library/react"
import * as React from "react"
import { AllowedTags, Data, DataVariants } from "./index"

afterEach(cleanup)

describe("<Data />", () => {
  it.todo("renders different variants when passed")
  it.todo("renders different tags when passed")
  it.todo("renders data unit as a prefix")
  it.todo("renders data unit as a suffix")
  it.todo("allows for rtl layouts for a prefix")
  it.todo("allows for rtl layouts for a suffix")
})
